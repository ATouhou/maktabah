extends base

block body
  ul.arab
    for category in categories
      li
        a(href='/category/' + category.id)= category.name
        ul
          for kitab in category.kutub
            li: a(href='/kitab/' + kitab.id)= kitab.name
